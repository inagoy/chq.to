<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title"><%= current_user.username %></h5>
    
  </div>
  <ul class="list-group list-group-flush">
    <li class="list-group-item">Email: <%= current_user.email %></li>
    <li class="list-group-item">You Have <%= current_user.links.count %> Links</li>
  </ul>
  <div class="card-body">
    <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete, :class => 'btn btn-light' %>
    <%= link_to 'Edit Profile', edit_user_registration_path, :class => 'btn btn-light' %>
  </div>
</div>